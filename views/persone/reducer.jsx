
//import addAction from './action';
//import { ADD_PERSONA } from './action';
import arrayReducer from "../commonsJSX/reducer";

const initialState = {
    elements:[{nome:"Aldo"}]
}

export default function personeReducer(state = initialState, action) {
   // if(action.name=="persone")
     //   return arrayReducer(state,action);
    //else
        return state;
}
